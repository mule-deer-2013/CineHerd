<%= form_for @post do |f| %>

  <%= f.label  :title %>
  <%= f.text_field  :title %>
  <%= f.label  :content %>
  <%= f.text_field  :content %>
  <%= hidden_field_tag 'question', params[:question] %>
  <%= f.submit "Create Post" %>

<% end %>
