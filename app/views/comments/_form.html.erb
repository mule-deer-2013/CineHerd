<%= form_for([@post, @comment], :html => {id: "form_comment_partial"}) do |f| %>
  <%= f.text_area  :content, placeholder: "Enter your comment here...", cols: 55, rows: 4 %></br>
  <%= f.submit "Create Comment" %>

<% end %>
